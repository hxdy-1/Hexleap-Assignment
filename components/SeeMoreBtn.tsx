import React from "react";

const SeeMoreBtn = () => {
	return (
		<button className="capitalize py-4 text-white px-8 bg-blue-500 font-semibold w-fit mx-auto rounded-md">
			See More
		</button>
	);
};

export default SeeMoreBtn;
